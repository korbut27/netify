package com.example.netify.repository.connection;

import com.example.netify.domain.connection.ConnectionApplication;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ConnecctionApplicationRepository extends JpaRepository<ConnectionApplication, Long> {
}
